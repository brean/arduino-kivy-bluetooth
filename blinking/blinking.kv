#:kivy 1.0.9

<Main>
    button: button
    lbl: lbl
    size_hint: (1, 1)
    size: (self.width, self.height)
    Label:
        id: lbl
    ToggleButton:
        id: button
        text: "OFF"
        on_press: root.toggle_blink()

